# Design Spec: Update luxury heading styling and wrapping

Update the final text overlay heading color to white and change its wrapping to render on two lines on both mobile and desktop.

## Design Details
* **Heading**: `Ready to Experience a New Line of Luxury?`
* **Heading color**: Change from gold/bronze (`text-[#D6B585]`) to white (`text-white`).
* **Heading wrapping (Mobile & Desktop Unified)**:
  * Line 1: `Ready to Experience`
  * Line 2: `a New Line of Luxury?`
